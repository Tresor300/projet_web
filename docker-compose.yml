services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: mariadb:10.11
    command: --lower-case-table-names=1
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: tv_fowet
    volumes:
      - ./db:/docker-entrypoint-initdb.d